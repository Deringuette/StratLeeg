@model IEnumerable<StrategicLegion.Models.StrategyModels.ChecklistModels.Checklist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChecklistName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateSubmitted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Submitter)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChecklistName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateSubmitted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Submitter)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChecklistId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChecklistId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChecklistId })
        </td>
    </tr>
}

</table>
